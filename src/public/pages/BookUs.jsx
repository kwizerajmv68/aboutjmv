import React from 'react';

const BookUs = () => {
  return (
    <div style={{padding:'4rem 5%', textAlign:'center'}}>
      <h1 style={{fontSize:'3rem', marginBottom:'2rem'}}>BOOK US</h1>
      <p style={{color:'var(--text-muted)'}}>Ready to start your project? Get in touch with us today.</p>
    </div>
  );
};

export default BookUs;
